export const colors = {
  // Brand accent — violet/indigo works great for AI products
  primary: {
    DEFAULT: "hsl(var(--primary))",
    foreground: "hsl(var(--primary-foreground))",
  },
  // You change ONLY the CSS variables below in globals.css
  // Everything else auto-updates
};
